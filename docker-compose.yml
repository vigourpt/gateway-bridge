version: '3.8'

services:
  gateway-bridge:
    build: .
    container_name: gateway-bridge
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - THOR_GATEWAY_URL=ws://187.124.114.207:45397
      - THOR_GATEWAY_TOKEN=MzLeAvE5uphx5w6WzwFHxJQdP1s4OalJ
      - LOCAL_GATEWAY_URL=ws://host.docker.internal:45397
    restart: unless-stopped
    networks:
      - openclaw-network

networks:
  openclaw-network:
    external: true
    name: openclaw-whdd_default